mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml
server:
  port: 8080
wyu:
  jwt:
    secret: f4e2e52034348f86b67cde581c0f9eb5
    expire: 604800
    header: Authorization
  file:
    staticAccessPath: /local/**
    uploadAvatar: D://img/
  paiUrl: http://localhost:8080/local/
## QQ邮箱配置
spring:
  mail:
    host: smtp.qq.com #发送邮件服务器
    username: 417724774@qq.com #发送邮件的邮箱地址
    password:  gjlblwixrurlbiff #客户端授权码，不是邮箱密码，这个在qq邮箱设置里面自动生成的
    properties.mail.smtp.port: 587 #端口号465或587
    from: 417724774@qq.com # 发送邮件的地址，和上面username一致
  #可以任意
  properties.mail.smtp.starttls.enable: true
  properties.mail.smtp.starttls.required: true
  properties.mail.smtp.ssl.enable: true
  default-encoding: utf-8
